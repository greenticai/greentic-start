# Auto-generated by sync-rnd-publish.sh — do not edit manually.
# Source: REPO_MANIFEST.toml in greenticai/.github
name: Rnd Publish

on:
  push:
    branches: [research]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Stage 1 — checks + version. Unlike the dev lane there is NO RUN_ID
  # stamping: the version is taken verbatim from Cargo.toml (X.Y.Z-research)
  # and validated for the -research suffix, then consumed by the binaries and
  # publish jobs below.
  rnd-prepare:
    uses: greenticai/.github/.github/workflows/rnd-prepare.yml@main
    secrets: inherit

  rnd-binaries-greentic-start:
    needs: rnd-prepare
    uses: greenticai/.github/.github/workflows/rnd-release-binaries.yml@main
    with:
      package: greentic-start
      version: ${{ needs.rnd-prepare.outputs.version }}
    secrets: inherit

  rnd-publish:
    needs: [rnd-prepare, rnd-binaries-greentic-start]
    uses: greenticai/.github/.github/workflows/rnd-publish.yml@main
    with:
      version: ${{ needs.rnd-prepare.outputs.version }}
      crates: "greentic-start"
      binary-crates: "greentic-start"
    secrets: inherit
